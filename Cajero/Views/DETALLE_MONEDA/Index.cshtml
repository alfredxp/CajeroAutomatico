@model IEnumerable<Cajero.Models.DETALLE_MONEDA>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BILLETE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CANTIDAD)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MAQUINA.ADDRESS_DESC)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BILLETE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CANTIDAD)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MAQUINA.ADDRESS_DESC)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_MONEDA_DETALLE }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_MONEDA_DETALLE }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_MONEDA_DETALLE })
        </td>
    </tr>
}

</table>
